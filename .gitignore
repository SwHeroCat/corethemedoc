# 1. Ignore all files and folders in the root directory
/*

# 2. STRICTLY IGNORE the .pi folder and all its subfolders/contents
/.pi/
/.pi/**

# 3. Allow Git to look inside other directories (to find 'docs/')
!/*/

# 4. Ignore the publish folder and all its contents
/publish/
/publish/**

# 5. Do not ignore the 'docs' folder itself
!/docs/

# 6. Do not ignore any content or subfolders inside 'docs'
!/docs/**

# 7. Ensure this .gitignore file itself is tracked and committed
!.gitignore